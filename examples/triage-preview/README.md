# Material UI triage preview

This is the permanent baseline project for disposable previews created by automated issue
triage. A triage pull request may change anything in this directory to demonstrate its fix.
Those changes are placed in a separate commit and should normally be removed before merge.

The baseline must remain buildable but intentionally does not demonstrate a reported issue.
